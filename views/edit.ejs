<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 p-10 text-white">
        <h5 class="mb-5">Edit post</h5>

        <form action="/update/<%= post._id %>" method="post">
           <textarea placeholder="" class="resize-none bg-transparent border-2 border-zinc-800 rounded-md p-3 outline-none w-1/3" name="content">
                <%= post.content %>
           </textarea>
           <input type="submit" value="Update post" class="px-3 w-30 py-2 bg-yellow-500 text-black block rounded-md mt-2 ">
        </form>

    </div>
    </div>
</body>
</html>